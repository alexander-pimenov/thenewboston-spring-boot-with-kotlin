### Паттерн AAA (Arrange-Act-Assert):
```kotlin
@Test
fun `should allow requests within limit`() {
// Arrange - подготовка данных
val userId = "user123"
val endpoint = "/api/test"
limitSystem.configureEndpoint(endpoint, config)

    // Act - выполнение действия
    val result = limitSystem.tryAcquire(userId, endpoint)

    // Assert - проверка результата
    assertTrue(result is LimitCheckResult.Approved)
}
```

### Хорошие практики именования тестов:
- should_expectedBehavior_when_stateUnderTest
- should_allow_requests_within_limit
- should_reject_requests_over_limit
- should_handle_concurrent_requests_correctly

### 🚀 ДЛЯ СОБЕСЕДОВАНИЯ ВАЖНО:
- Покрытие разных сценариев - happy path, edge cases, ошибки
- Многопоточные тесты - показывают понимание concurrency
- Интеграционные тесты - проверка взаимодействия компонентов
- Читаемые тесты - понятные имена и структура
- Изолированные тесты - каждый тест независим